# Foundry build artifacts
out/
cache/

# Broadcast logs (transactions, deployments)
broadcast/

# Dependencies (installed via forge install)
lib/

# Documentation
docs/

# Environment variables (private keys, API keys, RPC URLs)
.env
